import React from 'react'

const CreatePost = () => {
  return (
    <div>CreatePost</div>
  )
}

export default CreatePost



// update the component with two form input title and content use useRef after taking the entry post it to the 